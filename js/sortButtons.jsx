var SortButtons = React.createClass({
  render: function() {
    return (
      <div>
        <button id="sort">sort</button>
        <button id="sort" className="sortSave">Save</button>
      </div>
    );
  }
});

